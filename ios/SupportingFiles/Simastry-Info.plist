<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Merged with the generated Info.plist (GENERATE_INFOPLIST_FILE = YES).
	     CFBundleURLTypes cannot be expressed as an INFOPLIST_KEY_ build
	     setting, so it lives here. The simastry:// scheme powers widget
	     tap-through, notification routing, and invite links. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>simastry</string>
				<!-- SupabaseService builds the OAuth redirect from the bundle
				     identifier (app.rork.simastry://auth/callback); without
				     this scheme the browser can't return after Google
				     sign-in. -->
				<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
